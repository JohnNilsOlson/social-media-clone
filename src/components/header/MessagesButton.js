import React from "react";
import Button from "react-bootstrap/Button";

function MessagesButton() {
  return (
    <Button variant="primary" href="">Messages</Button>
  );
}

export default MessagesButton;